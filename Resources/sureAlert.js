function sureAlert() {
    var txt;
    if (confirm("Tem certeza disso?")) {
        alert("Inscrição enviada com sucesso!");
    }
}

function sureAlertCancelamento() {
    var txt;
    if (confirm("Tem certeza disso?")) {
        alert("Matrícula cancelada com sucesso!");
    }
}